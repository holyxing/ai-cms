# AI-CMS environment template (root)

Copy the deploy template and edit secrets locally:

```bash
cp deploy/.env.example deploy/.env
```

**Never commit `deploy/.env`.** Leave `*_API_KEY` empty unless you need live LLM calls.

See [SECURITY.md](./SECURITY.md).
